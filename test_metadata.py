from models.metadata import Metadata

metadata = Metadata(
    title="Learn Apple",
    description="Today's lesson teaches children about apples.",
    tags=["apple", "kids", "learning"]
)

print(metadata)